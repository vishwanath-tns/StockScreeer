# If you want to import scanner services directly, ensure PYTHONPATH includes the scanner project root
# Or set REST_API_BASE to point to a scanner REST wrapper
# Example: REST_API_BASE=http://localhost:5000
REST_API_BASE=
# Optional DB config if your client will connect directly
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DB=stockscreeer
MYSQL_USER=root
MYSQL_PASSWORD=
